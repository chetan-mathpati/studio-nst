# Studio NST Web Integration

From the Studio NST project root, merge these files and run:

```powershell
.\run_studio.ps1
```

Open `http://127.0.0.1:8000`.

The interface connects the existing Gatys, Johnson and AdaIN implementations to a local browser workspace with image upload, method controls, result metadata and original/result comparison.

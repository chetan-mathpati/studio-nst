# Experiments

This document is a living experiment log.

## Experiment E001 — Environment verification

**Status:** completed locally.

Record the exact output of:

```powershell
python scripts/check_environment.py
```

## Experiment E002 — Gatys step scaling

Planned configurations:

- 100 steps
- 300 steps
- 500 steps

Keep content/style images and maximum resolution fixed.

Measure:

- runtime,
- final losses,
- output image,
- GPU memory if available.

## Experiment E003 — Resolution scaling

Planned resolutions:

- 256 px
- 384 px
- 512 px

Keep method and optimization settings fixed.

## Experiment E004 — Cross-method comparison

Compare the three implementations using the same content/style pairs and document both quantitative measurements and qualitative observations.

The results section must only contain values generated by actual runs.
